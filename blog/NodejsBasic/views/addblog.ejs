<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.ckeditor.com/4.16.2/standard/ckeditor.js"></script>
</head>
<body>
    <%- include layout %>
    <div class="container">
        <h1 align="center">เขียนบทความ</h1>
        <%- messages() %>

            <% if(locals.errors) {  %>
            <ul class="alert alert-danger">
            <%  Object.values(errors).forEach(function(error){ %>
                <li><%= error.msg %></li>
                    <% }) %>
            </ul>
             <% } %>

        <form class="" action="/blog/add"method="post">
            <div class="form-group">
                <label for="">ชื่อบทความ</label>
                <input type="text" name="name"value="" class="form-control" placeholder="ระบุชื่อบทความ"> 
            </div>
            <div class="form-group">
                <label for="">รายละเอียดบทความ</label>
                <textarea name="description" rows="5" cols="80" class="form-control" placeholder="รายละเอียดบทความ"></textarea>
            </div>
            <div class="form-group">
                <label for="">ผู้แต่ง</label>
                <input type="text" name="author" class="form-control" placeholder="ระบุชื่อผู้แต่งได้ที่นี่....!"> 
            </div>
           <br> <input type="submit" value="บันทึกข้อมูล"class="btn btn-primary">

        </form>
    </div>
    <script>
        //jquery
        $(function(){
            $('#messages').addClass('alert alert-success');
        })
        //CKEDITOR 
        CKEDITOR.replace('description');
    </script>
</body>
</html>